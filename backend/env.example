# Environment Configuration Example
# Copy this file to .env for development or set these as environment variables in production

# Django Settings
SECRET_KEY=django-insecure-dfd@%i4g=of57mg6p78o9#^w=t2n1wb6)t^i77n$q&irklo+9c
DEBUG=True

# Hosts (comma-separated list)
ALLOWED_HOSTS=127.0.0.1,localhost,your-domain.com

# Database Configuration
# For development (SQLite): Leave DATABASE_URL empty or comment it out
# For production (PostgreSQL): Set DATABASE_URL
# DATABASE_URL=postgresql://username:password@host:port/database

# Example for production:
# DATABASE_URL=postgresql://timetracker_user:timetracker_pass@db:5432/timetracker

# CORS Settings (comma-separated list)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173,https://your-frontend-domain.com

# CSRF Settings (comma-separated list)
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173,https://your-frontend-domain.com,https://your-backend-domain.com 